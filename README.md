# About Project

This project uses a json server to create a simple REST API that stores a list of tasks (to do). It allows you to view the task list, add a new task, mark a task as completed and delete a task.

This project is made using React, TypeScript and Redux Toolkit Query.

